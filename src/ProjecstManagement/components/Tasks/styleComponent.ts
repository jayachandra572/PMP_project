import tw from 'tailwind.macro'
import styled from '@emotion/styled'
import { ProjectsContainer, ProjectsBox } from '../Projects/styleComponent'

export const TasksContainer = styled(ProjectsContainer)``
export const TasksBox = styled(ProjectsBox)``
