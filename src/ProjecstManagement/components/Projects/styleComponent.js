import tw from "tailwind.macro"
import styled from "@emotion/styled"
import Button from "../../../Common/components/Button"
import Colors from "../../themes/Colors"



export const ProjectsContainer = styled.div`${tw` p-8 flex flex-col `}
flex-grow:1;`
export const ProjectsBox = styled.div`${tw``}
flex-grow:1;

`
export const ProjectsWithNavigationBar = styled.div`${tw`relative`}`













