import tw from 'tailwind.macro'
import styled from '@emotion/styled'
import {
   ProjectHeaderContainer,
   ProjectTitle
} from '../ProjectHeader/styleComponent'

export const TasksHeaderContainer = styled(ProjectHeaderContainer)``
export const TasksTitle = styled(ProjectTitle)``
