import styled from '@emotion/styled'
import tw from 'tailwind.macro'

export const NoDataViewContainer = styled.div`
   ${tw`text-center m-4 max-h-screen max-w-screen flex justify-center`}
`

export const NoDataViewImg = styled.img`
   ${tw`h-full`};
`
